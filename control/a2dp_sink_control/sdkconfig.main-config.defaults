# Only the two relevant independent differences from the main IDF 6.1 build.
# Choice siblings and legacy aliases are generated by Kconfig, not copied.
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_240=y
CONFIG_BTDM_CTRL_MODEM_SLEEP=n
